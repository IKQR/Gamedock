@using GameDock.Shared.Dto

<div class="card">
        <div class="card-body">
            <p class="card-text">Name: @BuildInfo.Name</p>
            <p class="card-text">Version: @BuildInfo.Version</p>
            <p class="card-text">
                Status:&nbsp;
                @switch (BuildInfo.Status)
                {
                    case "Deleted":
                        <span class="text-danger">Deleted</span>
                        break;
                    case "Saved":
                        <span class="text-success">Saved</span>
                        break;
                    default:
                        <span class="text-info">Unknown</span>
                        break;
                }
            </p>
            <p class="card-text">
                Upload date: @BuildInfo.CreatedAt.ToLocalTime().ToString("G")
            </p>
        </div>
    </div>

@code {
    [Parameter]
    public BuildInfoDto BuildInfo { get; set; }
}